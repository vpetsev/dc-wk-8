<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Filtered Posts</title>
</head>
<body>
    <h3>Return to view all posts</h3>
    <button><a href="/posts">View all</a></button>
    <h3>Add a new blog post</h3>
    <button><a href="/add-post">Add A Post</a></button>
        <h4> Filter by Category</h4>
        <form method="post" action="/filter-posts">
            <select name="postCategory">
                <option value="JS">JS</option>
                <option value="PY">PY</option>
                <option value="TS">TS</option>
                <option value="Android">Android</option>
                <option value="IOs">IOs</option>
                <option value="C++">C++</option>
            </select>
            <button>Filter</button>
        </form>
    <ul>
        {{#filteredPosts}}
        <li>
        <form method="POST" action="/delete-post">
            <h4>{{title}} - {{category}}</h4>
            <p>{{body}}</p>
            <input type="hidden" value={{id}} name="postId"/>
            <button>Remove</button>
        </form>
        <button><a href="/post-details/{{id}}">See Full Post</a></button>
        </li>
        {{/filteredPosts}}
    </ul>
</body>
</html>